# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: Vũ Văn Hà
- Mã học viên: 2A202602589
- Nhóm: HelloWorld
- Repository/branch: task-data

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 1 — Thu thập tài liệu pháp luật | Thu thập 3 tài liệu về du lịch vào `data/landing/legal/`, đặt tên file không dấu và xử lý nguồn HTML thành file DOCX cục bộ khi cần. | `src/task1_collect_legal_docs.py` | Done |
| Task 2 — Crawl tin tức | Khai báo 5 URL tin du lịch, crawl bằng Crawl4AI hoặc fallback requests, lưu mỗi bài thành JSON có `url`, `title`, `date_crawled` và `content_markdown`. | `src/task2_crawl_news.py` | Done |
| Task 3 — Chuẩn hóa Markdown | Chuyển tài liệu pháp luật và bài viết JSON sang `data/standardized/{legal,news}`, giữ metadata nguồn, bỏ qua file đã xử lý và không tạo output rỗng. | `src/task3_convert_markdown.py` | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Dùng nguồn công khai về cùng chủ đề du lịch và lưu dữ liệu theo hai nhóm `legal` và `news`.  
   **Lý do/evidence:** Giúp corpus có cả văn bản pháp luật và tin tức, phù hợp yêu cầu tối thiểu của assignment; thể hiện trong `src/task1_collect_legal_docs.py` và `src/task2_crawl_news.py`.  
   **Trade-off:** Chất lượng nội dung phụ thuộc vào khả năng truy cập và cấu trúc HTML của từng nguồn.

2. **Quyết định:** Thiết kế fallback khi thu thập và chuẩn hóa dữ liệu.  
   **Lý do/evidence:** Task 2 dùng `requests` nếu Crawl4AI chưa cài; Task 3 đọc trực tiếp OpenXML của DOCX nếu MarkItDown không khả dụng.  
   **Trade-off:** Fallback ít mạnh hơn công cụ chuyên dụng với các trang hoặc định dạng phức tạp.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `pytest tests/test_acceptance.py -q`, tập trung vào số lượng tài liệu legal/news, metadata JSON và output Markdown.
- Kết quả trước/sau nếu có: Corpus có tối thiểu 3 tài liệu pháp luật, 5 bài viết JSON và Markdown ở cả hai thư mục `legal` và `news`.
- Lỗi đã phát hiện và cách xử lý: Bổ sung fallback khi thiếu Crawl4AI hoặc MarkItDown, đồng thời bỏ qua file đã tồn tại để tránh tạo bản trùng khi chạy lại.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Nội dung crawl phụ thuộc vào cấu trúc và khả năng truy cập của website nguồn; một số trang có thể chứa thêm phần điều hướng hoặc nội dung phụ.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Bổ sung kiểm tra chất lượng nội dung sau crawl và lưu log nguồn/thời điểm xử lý chi tiết hơn.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 2026-09-20
- Tên thành viên: Vũ Văn Hà
